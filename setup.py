from setuptools import setup

setup(
    name = "competitive_programming_backend",
    packages = ['competitive_programming_backend'],
    include_package_data = True,
    install_requires = [
        'flask'
    ],
)